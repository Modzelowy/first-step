# condition ? value_if_true : value_if_false
# Napisz program, który pobiera od użytkownika dwie liczby i wypisuje na ekranie większą z nich.

# def check_for_bigger_and_print(num_1, num_2):
#    print(num_1) if num_1 > num_2 else print(num_2)
# check_for_bigger_and_print(12,5)


# Napisz program, który pobiera od użytkownika imię i wiek,
# a następnie wyświetla na ekranie informację, czy użytkownik jest pełnoletni czy nie.
# dowiem się czy mogę w f stringu uzyc ternarny operator :D
# def check_for_age(name, age):
#    result = "adult" if age > 18 else "not an adult"
#    print(f"Hi {name} your age is {age} and you are {result} ")

# check_for_age("Andrzej", 13)

# Napisz program, który pobiera od użytkownika trzy liczby i wypisuje na ekranie informację,
# czy są one ułożone w kolejności rosnącej, malejącej czy nie są uporządkowane.
# def check_for_sort(arg1, arg2, arg3):
#    print("rosnaca") if arg1 < arg2 < arg3 else print("malejaco") if arg1 > arg2 > arg3  else print("nieuporzadkowane")
# check_for_sort(1,5,3)
a = 1
a = 1
